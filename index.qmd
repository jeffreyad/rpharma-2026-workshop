---
title: "Pharma Skills Workshop"
subtitle: "Hands-On AI Skills for Clinical Programming &amp; Biostatistics"
author:
  - name: "Jeff Dickinson, PhD"
    affiliations: "Associate Director, Clinical Reporting &middot; Navitas Data Sciences"
  - name: "Eric Zhang, PhD"
    affiliations: "Senior Director, Biostatistics &middot; Eikon Therapeutics"
date: "2026-10-01"
date-format: "long"
format:
  revealjs:
    theme: simple
    slide-number: true
    number-sections: true
    footer: "Pharma Skills Workshop &middot; R/Pharma Virtual 2026"
    toc: false
    width: 1200
    height: 720
    hash: true
---

# Welcome {.unnumbered}

## Pharma Skills Workshop

**Hands-On AI Skills for Clinical Programming & Biostatistics**

R/Pharma Virtual 2026 &middot; Thursday, October 1, 2026 &middot; 8:00–11:00 AM CDT &middot; Virtual

Presenters:

- **Jeff Dickinson, PhD** — Associate Director, Clinical Reporting, Navitas Data Sciences
- **Eric Zhang, PhD** — Senior Director, Biostatistics, Eikon Therapeutics

::: {.notes}
Virtual housekeeping to cover verbally: how to ask questions (chat vs.
unmute), how help works during hands-on time (breakout rooms / screen share),
and where the repo and environment links are posted (pin them in chat).

Three-hour block, 8:00–11:00 CDT. One break around the midpoint.

Opening framing: by the end you will have run two production-style
clinical workflows end to end by describing the task to an AI agent in plain
language — and you will have modified a skill and sketched one of your own.
:::

## What you'll do

- **Run AI skills on real-world exercises** — ADaM generation and group
  sequential design
- **See how skills are structured** and how they interact with the AI model
- **Practice using the skills**, with instructor support
- Leave with **practical ideas** for applying AI skills to your own work

*No prior AI-skills experience required.*

::: {.notes}
This mirrors the official session description. Emphasise the last two bullets —
this workshop is not just "watch the agent work," it's "learn to build the
thing that makes the agent reliable."

Repeat throughout: these skills produce *drafting aids, not validated
deliverables*. Independent QC still required before any regulated use.
:::

## Who this is for

- Statistical programmers
- Biostatisticians
- Clinical data scientists
- Anyone interested in practical AI applications in pharma R&D

Pick the exercises closest to your work — the ADaM thread, the group
sequential design thread, or both.

::: {.notes}
Set expectations: statisticians may go deep on Section A, programmers on
Section B, and everyone converges for the skill-authoring section. The
environment is preconfigured so no one loses time on setup.
:::

## Agenda

1. Skills overview — what they are, why they matter
2. Environment orientation — sign in, confirm you're ready
3. **Section A** — Group sequential design (Eric)
4. **Section B** — ADaM generation (Jeff)
5. **Section C** — Practice using the skills
6. Wrap-up — ideas to take back

Three hours, one break near the midpoint.

::: {.notes}
[Note: split of the 3-hour block.] Proposed, to confirm:

- 8:00–8:25  Welcome + skills overview
- 8:25–8:40  Environment orientation
- 8:40–9:35  Section A — group sequential design
- 9:35–9:45  Break
- 9:45–10:40 Section B — ADaM
- 10:40–10:55 Section C — modify / build a skill
- 10:55–11:00 Wrap-up
:::

# Skills overview

## What is a "skill"?

A **skill** is a reusable, model-agnostic workflow an AI coding agent loads
on demand:

- A `SKILL.md` file — step-by-step instructions for one class of task
- Supporting **reference files** — conventions, function-selection logic,
  failure modes
- Bundled **scripts** — R / Python the agent runs in your environment

The agent reads the skill only when your request matches it, then follows it
instead of improvising.

::: {.notes}
Contrast with a raw prompt: a prompt is transient and lives in one person's
head or chat history. A skill is version-controlled, reviewable, and encodes
institutional knowledge — "here is how *we* derive ADSL" — so every run starts
from the same vetted baseline.

Analogy: an SOP that the agent actually executes, not just reads.
:::

## Why skills matter for clinical programming

Clinical programming is high-stakes, convention-heavy, and traceability-driven.
A skill lets you encode, once:

- CDISC / ADaMIG conventions (`"Y"`/`NA` flags, study-day rules, date
  imputation direction)
- Function-selection logic (when `derive_vars_merged()` vs
  `derive_vars_merged_lookup()`)
- QC scaffolding (`stopifnot()` assertions, `# REVIEW:` markers at every
  protocol-specific decision)

The agent then reproduces that discipline on every run.

::: {.notes}
The point is consistency and traceability, not just speed. A skilled run
produces code that already has review markers where a human must make a
protocol call — the reviewer's attention is directed, not diffused.

Same argument on the biostatistics side: the group sequential design skill
encodes scope checks, the N-first algorithm, IA-timing constraints, and a
mandatory simulation-verification step.
:::

## Skills vs. no skills — group sequential design

`group-sequential-design` skill — 21 trial-design scenarios, 3 models
(Claude Sonnet / Opus, Gemini Flash), May 2026:

| | With skill | Without skill |
|---|---|---|
| Avg score across scenarios | **90%** | 62% |
| Scenario wins | **16** | 1 |
| Ties | 4 | 4 |



::: {.notes}
[EZ]: Updated the model run results
:::

## Skills vs. no skills — ADaM benchmarks {.smaller}

Community A/B benchmarks — **same task**, agent **with** vs **without** the
skill, scored against a methodology rubric:

| Skill / task | With skill | Without skill |
|---|---|---|
| ADSL derivation (34-point rubric) | **100%** | 62% |
| ADSL safety flags — SAFFL / ITTFL / PPROTFL | **83%** | 46% |
| ADAE derivation | **94%** | 33% |
| ADLB derivation | **94%** | 48% |
| ADVS (BDS) derivation | **88%** | 36% |

The gap is mostly **admiral-idiom compliance + QC annotations**, not whether
the numbers come out right.

::: {.notes}
Source: `benchmark-runner` results posted as comments on the `[benchmark]`
issues in RConsortium/pharma-skills — #137, #147, #148, #149, #126, #165.
Single runs, `claude-sonnet-4-6`, `pharmaversesdtm` → `pharmaverseadam` data,
May–Jul 2026; skill versions vary by run.

[Note: confirm these are the runs/numbers you want on-slide, and
whether to cite a specific skill SHA / date.]

What the gap actually is: both agents usually compute the *right values*. The
skill-guided agent uses admiral functions, places `# REVIEW:` comments at every
protocol-specific decision, and adds `stopifnot()` QC guards; the unaided agent
uses base dplyr/lubridate, no review markers, and sometimes leaves DOMAIN in or
keeps SDTM `"Y"/"N"` flags — code that would draw immediate QC findings.

Honest caveats to say out loud:

- On short rubrics or template-shaped tasks (basic two-arm ADSL, the admiral
  ADVS vignette pattern, trivial `derive_vars_cat` / `compute_rr` conversions)
  the measured gap is **~zero** — the base model already knows these.
- The skill runs roughly 1.5–9× more tokens.
- A couple of individual runs the skill scored slightly *worse* (e.g. duplicate
  baseline flags) — this is A/B benchmarking, not a guaranteed win every time.
:::

## The source project: pharma-skills

These skills come from the R Consortium **`pharma-skills`** project
(Submissions Working Group, Pilot 7; with the BBSW AI committee).

<https://github.com/RConsortium/pharma-skills/tree/main>

The full catalog also includes `sdtm-oak`, `clinical-trial-simulation`,
`statistical-reviewer`, `rounding`, and more, plus a skill development
**lifecycle** and community benchmarking.

::: {.notes}
Every skill in this workshop repo is copied from pharma-skills without changes
to its derivation logic. Credit: admiral skills — Jeff Dickinson (Navitas);
group-sequential-design — Eric Zhang.

Point people at LIFECYCLE.md if they want to contribute a skill — it connects
to Section C.
:::

## Three threads today

::: {.columns}
::: {.column width="33%"}
### A &middot; Group sequential design

Design a survival trial with **gsDesign / gsDesign2 / lrstat**:

- Boundaries, sample size, multiplicity
- Simulation verification + Word report
:::
::: {.column width="33%"}
### B &middot; ADaM generation

Derive analysis datasets from SDTM with **{admiral}**:

- `admiral` (parent)
- `admiral-adsl` → `admiral-adae` → `admiral-bds`
:::
::: {.column width="33%"}
### C &middot; practice with the skills

- try skills with an example
- Sketch your own
:::
:::

::: {.notes}
All three share one interaction model: describe the task in natural language,
the agent loads the matching skill, you review what it produces. The domains
differ; the workflow does not.
:::

# Environment orientation

## No setup required

The workshop runs in a **preconfigured environment** with:

- Access to the AI model, already wired up
- A library of pharma-focused skills, pre-installed
- All R and Python packages for both threads, already installed

You do **not** need to install anything or configure anything in advance.

::: {.notes}
This is straight from the session description — "participants will work in a
preconfigured environment ... without any advance setup." The next few slides
are about *getting in* and *confirming*, not installing.
:::

## 1. Sign in

Go to **<https://workshop.posit.team/>** and sign in.

- Choose **Sign in with OpenID** *or* **Sign in with GitHub**
- Use the same account for the whole session

::: {.notes}
Walk the room through it live in the webinar; confirm everyone is in via chat
before moving on. If GitHub sign-in fails, try the OpenID option.

[Note: is there a per-attendee access code / invite, or is it
self-service GitHub/OpenID only? Confirm the exact wording of the sign-in
options shown on workshop.posit.team.]
:::

## 2. Fork the workshop repo

On GitHub, fork **jeffreyad/rpharma-2026-workshop** to your own account.

- Section C has you modify and sketch a skill — forking gives you somewhere
  of your own to keep that work after the session ends
- If you signed in above with GitHub, you already have an account — one extra
  click. **No GitHub account?** Sign up free at
  **<https://github.com/signup>** (about a minute) — you'll need one for this
  step regardless of how you signed in to Workbench
- Optional stretch: open a PR back to the source **pharma-skills** project

::: {.notes}
[Note: is this step actually needed, or does the preconfigured
environment already persist each attendee's work some other way (e.g. a
per-user workspace that survives session end)? If it does, cut this slide —
don't add GitHub mechanics to a 3-hour session unless they buy something.

If we keep it: fork on github.com takes a few clicks and can happen before or
while Positron is loading. The next step then clones *the attendee's fork*,
not the upstream repo, so `git push` works without a permissions prompt.

Worth flagging in chat at the top of orientation: anyone who signed in via
OpenID (not GitHub) will need a free GitHub account for this step specifically
— point them to github.com/signup before you start walking through the fork.]
:::

## 3. Open your Positron session

In Workbench, start the session and choose the **Positron** IDE.

**Why Positron?** One IDE, **both R and Python** — Section A's report
generator is Python (`python-docx`), Section B is R (admiral), and Section C
touches both. One session covers everything.

``` bash
git clone https://github.com/<your-username>/rpharma-2026-workshop.git
```

::: {.notes}
[Note: does each attendee land in a pre-cloned copy already, or
clone their own fork as shown here? If pre-cloned, add a step to point the
`origin` remote at their fork (or add it as a second remote) so Section C work
can still be pushed somewhere persistent:
`git remote set-url origin https://github.com/<your-username>/rpharma-2026-workshop.git`]
:::

## 4. Confirm you're ready

From the repo root:

``` r
source("check_setup.R")
```

Prints `[ OK ]` / `[WARN]` / `[FAIL]` per line for:

- R &ge; 4.2 and the admiral set (`admiral` &ge; 1.2.0, `dplyr`, `lubridate`,
  `pharmaversesdtm`, `pharmaverseadam`, `metacore`, `xportr`)
- The GSD set (`gsDesign`, `gsDesign2`, `lrstat`, `graphicalMCP`, `jsonlite`)
- Python &ge; 3.8 with `python-docx`

In the preconfigured environment every line should be **`[ OK ]`**. Any
`[FAIL]` prints the exact fix — usually:

``` r
Rscript install_packages.R
```

::: {.notes}
`check_setup.R` is base-R only and installs nothing — it just reports and
tells you what to run. `install_packages.R` only installs what's missing or
outdated, so it's safe to let attendees try the printed fix once, even in a
preconfigured environment with an unexpected gap.

If it doesn't clear in a minute or two, or the failure is Python or R itself
(`install_packages.R` can't fix either — `check_setup.R` says so explicitly),
stop there: flag it in chat rather than troubleshooting solo. That's an
environment issue for us to fix live, not something to debug on your own time.
:::

## You should now see…

::: {.callout-tip}
Ready-state checklist
:::

- Positron open in your browser, with **your fork** of the workshop repo in
  the Explorer
- `check_setup.R` output showing **all checks passed** (no red)
- An R console and a Python console both available

::: {.notes}
[Note: replace with an actual screenshot of the ready state, or a
precise description. Consider sharing a reference screenshot in chat so people
can self-check without unmuting.]

Do not start Section A until everyone confirms this in chat.
:::

# Section A — Group sequential design {.unnumbered}

## Section A — what we'll do

Drive the **group-sequential-design** skill to design a survival trial:

- Survival endpoints (OS / PFS / DFS), interim + final analyses
- Alpha-spending boundaries (Lan-DeMets, Hwang-Shih-DeCani)
- Sample-size / event targets (N-first approach)
- Graphical multiplicity for co-primary / multi-population designs
- **`lrstat` simulation** to verify the analytic design
- A template-driven **Word report** (`gsd_report.py`)

::: {.notes}
Scope note the skill enforces: standard group sequential designs only. Adaptive
enrichment, sample-size re-estimation, seamless dose-selection, platform trials,
and non-survival primary endpoints are explicitly out of scope — the skill will
say so and stop.

Tools: gsDesign, gsDesign2, lrstat, graphicalMCP, jsonlite (R) + python-docx.

[Note: time budget for Section A within the 3-hour block.]
:::

## Section A — how the skill behaves

- Asks design questions **one at a time**, in plain language, no defaults
  offered
- Summarises all inputs in a table and waits for your confirmation
- Computes the design, then **checks interim-analysis timing** against
  enrollment and minimum-gap constraints
- **Verifies by simulation** before delivering — power within ±2 pp, type I
  error within ±0.5 pp
- Writes every number to `gsd_results.json`; the report reads from there (no
  hardcoded values)

::: {.notes}
The one-artifact-per-turn pacing in the skill means the agent writes a script,
stops, runs it, stops, etc. That is intentional — it keeps each step
reviewable.
:::

## Section A — hands-on exercise

Let's design a Phase 3 Oncology trial



::: {.notes}
FLAG: this section needs Eric's direct input and review. Do not draft a
scenario — the Q&A flow, the specific numbers, and the "what good looks like"
checkpoints should be Eric's. Once provided, expand into full step slides
matching the what-we'll-do / how-it-behaves / hands-on / debrief pattern used
elsewhere in this deck.
:::

## Section A — debrief

- Are the questions asked by the skill helpful?
- Do the outputs include everything you need?
- Can you apply this skill to your workflow?

::: {.notes}
Same disclaimer applies: a designed-and-verified draft is still a draft. The
statistician owns the final design.
:::

# Section B — ADaM generation {.unnumbered}

## Section B — what we'll do

Drive the **admiral** skill family to derive ADaM datasets from SDTM:

| Skill | Produces | Depends on |
|---|---|---|
| `admiral` (parent) | Shared conventions — no output | — |
| `admiral-adsl` | ADSL (subject-level) | SDTM DM, EX, DS |
| `admiral-adae` | ADAE (adverse events, TEAE flag) | ADSL + SDTM AE |
| `admiral-bds` | ADVS / ADLB (BDS findings) | ADSL + SDTM VS / LB |

**ADSL must be derived first** — its treatment vars and population flags feed
every downstream dataset.

::: {.notes}
Source data is the pharmaverse public CDISC test data (`pharmaversesdtm` /
`pharmaverseadam`) — no proprietary data, safe to run live.

The agent reads `skills/admiral/SKILL.md` (shared conventions) first, then the
child skill for the dataset in question.

[Note: time budget for Section B within the 3-hour block.]
:::

## Section B — how the skill behaves

When you ask for a derivation, the skilled agent will:

- Generate code **section by section**, not one giant block
- Use the native pipe `|>` and `exprs()` for admiral verb arguments
- Use admiral date functions (never `as.Date()` on `--DTC`)
- Emit `"Y"`/`NA` flags — never `"N"`
- Drop a **`# REVIEW:`** comment at every protocol-specific decision
- Add `stopifnot()` assertions (one row per USUBJID, source uniqueness, …)

::: {.notes}
Tell attendees what to look for when reviewing: the `# REVIEW:` markers are
where *they* — not the agent — own the decision (population flag definitions,
disposition mapping, age cut-points, TEAE window). The skill deliberately does
not guess these.
:::

## Section B — hands-on exercise

**Step 1 — everyone: derive ADSL**

Prompt to give the agent (verbatim, in chat — not typed as code):

> "Using the admiral-adsl skill, derive ADSL from the pharmaversesdtm DM, EX,
> and DS domains. Include TRT01P/TRT01A, treatment start/end dates, and the
> SAFFL population flag."

Watch for: the skill should ask you to confirm the treatment variable mapping
and the SAFFL definition before it writes any code — if it doesn't ask,
that's worth flagging in debrief.

**Step 2 — pick a track**

- **Track 1 — ADAE:** "Using the admiral-adae skill and the ADSL you just
  built, derive ADAE from the pharmaversesdtm AE domain. Include the TEAE
  flag and AE start/end study day."
- **Track 2 — BDS (ADVS):** "Using the admiral-bds skill and the ADSL you
  just built, derive ADVS from the pharmaversesdtm VS domain. Include
  baseline flag and change-from-baseline for each parameter."

Both tracks depend on the ADSL from Step 1 — point people to their own
output, not a shared copy, so the exercise mirrors a real pipeline.

::: {.notes}
People self-select a track based on which is closer to their own work —
programmers who touch safety data lean ADAE, anyone doing efficacy/labs leans
BDS. No wrong choice; the debrief afterward works for either.
:::

## Section B — what "done" looks like

- **ADSL:** one row per USUBJID; TRT01P/TRT01A populated for all subjects;
  SAFFL is `"Y"`/`NA`, never `"N"`; `stopifnot()` guard for row uniqueness
  passes
- **ADAE (Track 1):** TEAE flag correctly excludes pre-treatment AEs; AE
  start day computed relative to treatment start, not enrollment; at least
  one `# REVIEW:` comment on the TEAE window definition
- **ADVS (Track 2):** baseline flag set to the last non-missing value
  on/before treatment start per parameter; change-from-baseline present for
  post-baseline records only

If your output is missing a `# REVIEW:` comment somewhere you'd expect one
(TEAE window, baseline definition, population flag edge case), that's the
thing to raise in debrief — not a bug to fix silently.

::: {.notes}
Use this slide as a self-check people can glance at while working, not
something to read through line by line. Circulate and use it as your own
checklist when helping someone who's stuck.
:::

## Section B — stretch goal

Finished early? Two options:

- Run the *other* track (ADAE if you did BDS, or vice versa)
- Open `skills/admiral/admiral-adae/SKILL.md` (or
  `skills/admiral/admiral-bds/SKILL.md`) and find the section defining the
  TEAE window / baseline rule — see whether the agent's `# REVIEW:`
  placement matches what the skill actually says to flag

::: {.notes}
The second option is the more interesting one for people who want to see
behind the curtain — it's the first time in the workshop they read the skill
file itself rather than just its output.
:::

## Section B — debrief

- Where did the agent place `# REVIEW:` markers, and were they the right places?
- What did *you* have to decide that the skill correctly refused to guess?
- How does this compare to writing the derivation from scratch?

::: {.notes}
Reinforce the disclaimer: the output is a drafting aid. Independent QC against
your own process is still required before any regulated use.
:::

# Section C — Practice with group-sequential-design and admiral {.unnumbered}

## Now it's your turn

Try one of today's skills on something from your own work:

- **group-sequential-design** — describe a trial you're actually designing
- **admiral** (`admiral-adsl` / `admiral-adae` / `admiral-bds`) — point it at
  a dataset you work with

Just describe the task in plain language and see what the agent produces.
Commit anything you want to keep to **your fork**. We're here to help if you
get stuck.

::: {.notes}
Instructor-supported open practice time — no fixed script. Circulate and help
people get unstuck rather than walking through a set scenario. Afterward, ask
a couple of people to share what they tried and what stood out, then move to
wrap-up.

[Note: if the "fork the repo" step (Environment orientation, #2)
is cut, swap "commit anything you want to keep to your fork" for however work
actually persists — e.g. "download what you want to keep" or "it stays in
your Workbench home directory."]
:::

# Wrap-up

## What you did today

- Ran the **group-sequential-design** skill to design and verify a survival
  trial
- Ran the **admiral** skills to derive ADaM datasets from SDTM
- Read a skill's internals, **modified** one, and **sketched** your own

All driven by describing the task in natural language — the skill supplied the
discipline.

::: {.notes}
Land the through-line: the value is not "AI writes code fast." It is that a
reviewed, version-controlled workflow runs consistently every time, with review
markers exactly where human judgment is required — and that you can now build
those workflows yourself.
:::

## Take it with you

- **Your fork** — has your Section C work; keep building on it
- **Workshop repo:** <https://github.com/jeffreyad/rpharma-2026-workshop>
- **pharma-skills:** <https://github.com/RConsortium/pharma-skills/tree/main>
  — full catalog, LIFECYCLE.md, benchmarking
- **admiral:** <https://pharmaverse.github.io/admiral/> ·
  [CDISC ADaMIG v1.3](https://www.cdisc.org/standards/foundational/adam)
- **gsDesign / gsDesign2 / lrstat:** on CRAN
- **Submissions Working Group:** <https://rconsortium.github.io/submissions-wg/>

Install into your own project:

``` bash
npx skills add RConsortium/pharma-skills --skill admiral-adsl --skill group-sequential-design
```

Running the workshop repo itself on your own machine (rather than the cloud
environment)? Your fork has `install_packages.R` + `check_setup.R` and a full
troubleshooting guide in `INSTALL.md`.

::: {.notes}
Also mention Option A from the repo README — open the repo as your project and
let its CLAUDE.md route the agent to each skill; set your local Rscript path
for the GSD skill.
:::

## Disclaimer

These skills produce **drafting aids, not validated deliverables**.

LLM output can be wrong or vary between runs. Independently verify all
generated code and results against your own QC process before they inform any
regulatory, clinical, safety, or GxP-regulated activity, and comply with your
organization's policies.

The skills bundle scripts that execute in your environment — review a skill's
contents before running it, and only supply data you are permitted to use.

::: {.notes}
Say this one out loud, verbatim if needed. It is the single most important
slide for a pharma audience.
:::

## Thank you

**Jeff Dickinson, PhD**
Associate Director, Clinical Reporting · Navitas Data Sciences
[Note: contact — email / GitHub / LinkedIn]

**Eric Zhang, PhD**
Senior Director, Biostatistics · Eikon Therapeutics
[Note: contact — email / GitHub / LinkedIn]

Questions?

::: {.notes}
Leave this up during Q&A. Post the repo URL and the pharma-skills URL in chat
as a pinned message for people photographing / screenshotting slides.
:::
